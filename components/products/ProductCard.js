import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({}));

function ProductCard({ products }) {
  return <h1>Product Card View to implement</h1>;
}

export default ProductCard;
